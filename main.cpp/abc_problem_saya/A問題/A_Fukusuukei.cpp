#include <bits/stdc++.h>
using namespace std;

int main(){
    string W;
    cin >> W;
    cout << W + 's' << endl;
}

/*
string Wを受け取る
そのままs足して出力
簡単すぎないか？？
足し方は、上記のように+を使っても、
　　　　　W << 's'としても、メモリは変わらない
*/